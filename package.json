{
    "name": "bloglist",
    "version": "1.0.0",
    "description": "bloglist application for fullstackopen",
    "main": "server/index.js",
    "scripts": {
        "test": "NODE_ENV=test jest --verbose --runInBand",
        "start-prod": "NODE_ENV=production node server/index.js",
        "dev": "NODE_ENV=developement nodemon index.js",
        "install:client": "cd ./client && npm install",
        "build:client": "cd ./client && npm run build",
        "start": "npm install:client && npm run build:client && npm start"
    },
    "author": "David Gvetadze",
    "license": "ISC",
    "devDependencies": {
        "eslint": "^7.11.0",
        "jest": "^26.6.0",
        "nodemon": "^2.0.6",
        "supertest": "^5.0.0"
    },
    "dependencies": {
        "bcrypt": "^5.0.0",
        "cors": "^2.8.5",
        "dotenv": "^8.2.0",
        "express": "^4.17.1",
        "jsonwebtoken": "^8.5.1",
        "mongoose": "^5.10.9",
        "mongoose-unique-validator": "^2.0.3"
    },
    "jest": {
        "testEnvironment": "node"
    }
}
